from pyfiglet import Figlet


def main():
    text = input("Input: ").strip()

    length = len(text)

    print(f"number of args: {length}")

    if len(text) == 1:
        ...

    if len(text) == 3:
        ...

if __name__ == "__main__":
    main()